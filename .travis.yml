language: node_js

node_js:
  - "node"

services:
  - postgresql

before_install:
  - psql -U postgres < test_db.dump
  - curl -o- -L https://yarnpkg.com/install.sh | bash
  - export PATH="$HOME/.yarn/bin:$PATH"

install:
  - yarn install --ignore-scripts --ignore-engines
  - cd client && yarn install --ignore-scripts --ignore-engines

script:
  - yarn lint
  - yarn lint:client
  - yarn test
